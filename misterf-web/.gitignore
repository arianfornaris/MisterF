node_modules/
.npm-cache/
tmp/
.env
.env.*
!.env.example
/data.sqlite
/data.sqlite-*
data/**/*.sqlite
data/**/*.sqlite-*
data/**/*.db
data/**/*.db-*
npm-debug.log*
.DS_Store

# The unhashed theme copy exists only so the static kitchen sink can link it
# (a plain HTML file cannot resolve a hashed name). It is a byte-for-byte
# duplicate of the hashed build, regenerated by `npm run build:client`.
public/build/css/cuaderno.css
