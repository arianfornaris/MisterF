<%- include('partials/app-shell-open') %>

<section class="app-resource-view assignments-page">
  <div class="resource-page-header">
    <div>
      <p class="resource-eyebrow">Tarea compartida</p>
      <h1><%= selectedAssignment.title %></h1>
    </div>
    <form method="post" action="/resources/shared/<%= encodeURIComponent(shareLink.id) %>/accept">
      <input type="hidden" name="_csrf" value="<%= csrfToken %>">
      <button class="btn btn-primary" type="submit">
        <i class="bi bi-plus-circle me-1" aria-hidden="true"></i>Agregar a mis recursos
      </button>
    </form>
  </div>

  <div class="resource-detail-summary">
    <div class="resource-markdown mb-2" data-render-markdown><%= selectedAssignment.description || draft.targetTopic || 'Sin descripción' %></div>
    <div class="resource-meta-row">
      <% if (draft.targetTopic) { %><span><i class="bi bi-bullseye" aria-hidden="true"></i> <%= draft.targetTopic %></span><% } %>
      <% if (draft.level) { %><span><i class="bi bi-bar-chart" aria-hidden="true"></i> <%= draft.level %></span><% } %>
      <span><i class="bi bi-list-check" aria-hidden="true"></i> <%= draft.blocks.length %> bloques</span>
    </div>
  </div>

  <div data-assignment-quiz-host data-assignment-quiz-mode="preview"></div>
  <div class="assignment-block-list" data-assignment-quiz-fallback>
    <% draft.blocks.forEach((block, index) => { %>
      <%- include('partials/assignment-item-card', { assignmentItemMode: 'preview', block, csrfToken, draft, index, selectedAssignment: null }) %>
    <% }) %>
  </div>
</section>

<script type="application/json" data-assignment-quiz-json><%- assignmentQuizJson %></script>

<%- include('partials/app-shell-close', { pageScriptPartial: 'assignments-client-script' }) %>
