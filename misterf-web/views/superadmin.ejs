<!doctype html>
<html lang="<%= htmlLang %>">
  <head>
    <%- include('partials/document-head', { title: 'Superadmin', includeBootstrapIcons: false }) %>
  </head>
  <body>
    <main class="superadmin-shell">
      <section class="superadmin-sheet">
        <header class="superadmin-header">
          <div>
            <p class="superadmin-kicker">Superadmin</p>
            <h1><%= t('superadmin.users') %></h1>
          </div>
          <a class="btn btn-link px-0" href="/"><%= t('superadmin.backToChat') %></a>
        </header>

        <% if (success) { %>
          <div class="alert alert-success" role="alert"><%= success %></div>
        <% } %>

        <% if (error) { %>
          <div class="alert alert-danger" role="alert"><%= error %></div>
        <% } %>

        <div class="row g-4">
          <aside class="<%= mode === 'detail' ? 'col-lg-5' : 'col-12' %>">
            <div class="card superadmin-card">
              <div class="card-body">
                <div class="d-flex align-items-center justify-content-between gap-3 mb-3">
                  <h2 class="h5 mb-0"><%= t('superadmin.allUsers') %></h2>
                  <span class="badge text-bg-light"><%= users.length %></span>
                </div>

                <% if (users.length === 0) { %>
                  <div class="alert alert-light mb-0" role="status">
                    <%= t('superadmin.noUsers') %>
                  </div>
                <% } else { %>
                  <div class="list-group superadmin-user-list">
                    <% users.forEach((user) => { %>
                      <a
                        class="list-group-item list-group-item-action <%= activeUser && activeUser.id === user.id ? 'active' : '' %>"
                        href="/superadmin/users/<%= encodeURIComponent(user.id) %>"
                      >
                        <span class="d-flex w-100 justify-content-between gap-3">
                          <strong><%= user.fullName %></strong>
                          <small><%= formatDate(user.createdAt) %></small>
                        </span>
                        <span class="d-block small <%= activeUser && activeUser.id === user.id ? 'text-white-50' : 'text-muted' %>">
                          <%= user.email %>
                        </span>
                      </a>
                    <% }) %>
                  </div>
                <% } %>
              </div>
            </div>
          </aside>

          <% if (mode === 'detail' && activeUser) { %>
            <section class="col-lg-7">
              <div class="vstack gap-4">
                <div class="card superadmin-card">
                  <div class="card-body">
                    <h2 class="h5"><%= t('superadmin.userData') %></h2>
                    <dl class="row superadmin-dl mb-0">
                      <dt class="col-sm-4"><%= t('superadmin.name') %></dt>
                      <dd class="col-sm-8"><%= activeUser.fullName %></dd>
                      <dt class="col-sm-4"><%= t('superadmin.email') %></dt>
                      <dd class="col-sm-8"><%= activeUser.email %></dd>
                      <dt class="col-sm-4"><%= t('superadmin.emailVerified') %></dt>
                      <dd class="col-sm-8"><%= activeUser.emailVerified ? t('common.yes') : t('common.no') %></dd>
                      <dt class="col-sm-4"><%= t('superadmin.profile') %></dt>
                      <dd class="col-sm-8">
                        <% if (activeUser.identityProviders.length === 0) { %>
                          <span class="text-muted"><%= t('superadmin.noProvider') %></span>
                        <% } else { %>
                          <% activeUser.identityProviders.forEach((provider) => { %>
                            <span class="badge text-bg-secondary me-1"><%= provider %></span>
                          <% }) %>
                        <% } %>
                      </dd>
                      <dt class="col-sm-4"><%= t('superadmin.created') %></dt>
                      <dd class="col-sm-8"><%= formatDate(activeUser.createdAt) %></dd>
                      <dt class="col-sm-4"><%= t('superadmin.updated') %></dt>
                      <dd class="col-sm-8"><%= formatDate(activeUser.updatedAt) %></dd>
                      <dt class="col-sm-4">ID</dt>
                      <dd class="col-sm-8"><code><%= activeUser.id %></code></dd>
                    </dl>
                  </div>
                </div>

                <div class="card superadmin-card" id="openrouter-key">
                  <div class="card-body">
                    <div class="d-flex align-items-start justify-content-between gap-3 mb-3">
                      <div>
                        <h2 class="h5 mb-1">OpenRouter key</h2>
                        <p class="text-muted mb-0">
                          <%= t('superadmin.keyInfoCopy') %>
                        </p>
                      </div>
                      <% if (keyRecord) { %>
                        <span class="badge text-bg-<%= keyRecord.status === 'active' ? 'success' : keyRecord.status === 'error' ? 'danger' : 'warning' %>">
                          <%= keyRecord.status %>
                        </span>
                      <% } %>
                    </div>

                    <% if (!keyRecord) { %>
                      <div class="alert alert-warning mb-0" role="alert">
                        <%= t('superadmin.noKey') %>
                      </div>
                    <% } else { %>
                      <dl class="row superadmin-dl">
                        <dt class="col-sm-4"><%= t('superadmin.name') %></dt>
                        <dd class="col-sm-8"><%= keyRecord.name %></dd>
                        <dt class="col-sm-4">Hash</dt>
                        <dd class="col-sm-8">
                          <% if (keyRecord.keyHash) { %>
                            <a href="#manage-openrouter-key"><code><%= keyRecord.keyHash %></code></a>
                          <% } else { %>
                            <span class="text-muted"><%= t('superadmin.pending') %></span>
                          <% } %>
                        </dd>
                        <dt class="col-sm-4"><%= t('superadmin.localLimit') %></dt>
                        <dd class="col-sm-8"><%= formatMoney(keyRecord.limitUsd) %></dd>
                        <dt class="col-sm-4"><%= t('superadmin.localReset') %></dt>
                        <dd class="col-sm-8"><%= keyRecord.limitReset || t('superadmin.noReset') %></dd>
                        <dt class="col-sm-4"><%= t('superadmin.updatedFem') %></dt>
                        <dd class="col-sm-8"><%= formatDate(keyRecord.updatedAt) %></dd>
                      </dl>

                      <% if (keyRecord.lastError) { %>
                        <div class="alert alert-danger" role="alert">
                          <strong><%= t('superadmin.lastError') %></strong> <%= keyRecord.lastError %>
                        </div>
                      <% } %>

                      <% if (remoteError) { %>
                        <div class="alert alert-warning" role="alert">
                          <%= t('superadmin.remoteReadError') %> <%= remoteError %>
                        </div>
                      <% } %>

                      <% if (openRouterInfo) { %>
                        <div class="table-responsive mb-4">
                          <table class="table table-sm align-middle mb-0">
                            <tbody>
                              <tr>
                                <th scope="row"><%= t('superadmin.limit') %></th>
                                <td><%= formatMoney(openRouterInfo.limit) %></td>
                              </tr>
                              <tr>
                                <th scope="row"><%= t('superadmin.available') %></th>
                                <td><%= formatMoney(openRouterInfo.limitRemaining) %></td>
                              </tr>
                              <tr>
                                <th scope="row"><%= t('superadmin.usage') %></th>
                                <td><%= formatMoney(openRouterInfo.usage) %></td>
                              </tr>
                              <tr>
                                <th scope="row"><%= t('superadmin.usageMonthly') %></th>
                                <td><%= formatMoney(openRouterInfo.usageMonthly) %></td>
                              </tr>
                              <tr>
                                <th scope="row"><%= t('superadmin.reset') %></th>
                                <td><%= openRouterInfo.limitReset || t('superadmin.noReset') %></td>
                              </tr>
                              <tr>
                                <th scope="row"><%= t('superadmin.disabledFem') %></th>
                                <td><%= openRouterInfo.disabled ? t('common.yes') : t('common.no') %></td>
                              </tr>
                            </tbody>
                          </table>
                        </div>
                      <% } %>

                      <form
                        id="manage-openrouter-key"
                        class="superadmin-key-form"
                        method="post"
                        action="/superadmin/users/<%= encodeURIComponent(activeUser.id) %>/openrouter-key"
                      >
                        <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                        <h3 class="h6"><%= t('superadmin.manageBalance') %></h3>

                        <div class="row g-3">
                          <div class="col-sm-6">
                            <label class="form-label" for="limitUsd"><%= t('superadmin.limitUsd') %></label>
                            <input
                              id="limitUsd"
                              class="form-control"
                              name="limitUsd"
                              type="number"
                              min="0"
                              step="0.0001"
                              value="<%= openRouterInfo && openRouterInfo.limit !== null && openRouterInfo.limit !== undefined ? openRouterInfo.limit : keyRecord.limitUsd ?? '' %>"
                              placeholder="<%= t('superadmin.noLimit') %>"
                            >
                          </div>

                          <div class="col-sm-6">
                            <label class="form-label" for="limitReset"><%= t('superadmin.reset') %></label>
                            <select id="limitReset" class="form-select" name="limitReset">
                              <% const currentReset = openRouterInfo?.limitReset ?? keyRecord.limitReset ?? ''; %>
                              <option value="" <%= !currentReset ? 'selected' : '' %>><%= t('superadmin.noResetOption') %></option>
                              <option value="daily" <%= currentReset === 'daily' ? 'selected' : '' %>><%= t('superadmin.daily') %></option>
                              <option value="weekly" <%= currentReset === 'weekly' ? 'selected' : '' %>><%= t('superadmin.weekly') %></option>
                              <option value="monthly" <%= currentReset === 'monthly' ? 'selected' : '' %>><%= t('superadmin.monthly') %></option>
                            </select>
                          </div>

                          <div class="col-12">
                            <div class="form-check">
                              <input
                                id="disabled"
                                class="form-check-input"
                                name="disabled"
                                type="checkbox"
                                <%= openRouterInfo?.disabled ? 'checked' : '' %>
                              >
                              <label class="form-check-label" for="disabled">
                                <%= t('superadmin.disableThisKey') %>
                              </label>
                            </div>
                          </div>
                        </div>

                        <button class="btn btn-primary mt-3" type="submit">
                          <%= t('common.saveChanges') %>
                        </button>
                      </form>
                    <% } %>
                  </div>
                </div>
              </div>
            </section>
          <% } %>
        </div>
      </section>
    </main>
  </body>
</html>
