Your previous response to the quiz revision request was rejected.

{{CORRECTION_REASON}}

{{REVISION_SCOPE_RULES}}

Re-emit the corrected result as one JSON object with exactly the keys your scope
allows, and nothing else. Do not apologize, explain, or wrap the JSON in
markdown fences.

Keep everything from your previous attempt that was already correct. Return
complete lists and objects, including unchanged entries, and preserve the `id`
of every block and section you keep.

Write learner-facing and teacher-facing text in {{INSTRUCTION_LANGUAGE_NAME}}.{{QUIZ_SUPPORT_LANGUAGE_RULES}}
